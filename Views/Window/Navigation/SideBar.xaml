<UserControl x:Class="AccountManager.Views.Window.Navigation.Sidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:navigationViews="clr-namespace:AccountManager.Views.Window.Navigation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             DataContext="{Binding SidebarViewModel}">

    <Border Background="{DynamicResource SidebarColor}" CornerRadius="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Logo and Title in Sidebar Area -->
            <navigationViews:SidebarTop Grid.Row="0"/>
            
            <!-- Groups Header -->
            <navigationViews:SidebarTopActions Grid.Row="1"/>

            <!-- Groups List Container -->
            <ScrollViewer Grid.Row="2" 
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         Margin="24,0,24,0">
                <StackPanel>

                    <!-- 1. TOP SYSTEM GROUPS: All Items, Favorites -->
                    <navigationViews:SystemGroupsList x:Name="TopSystemGroupsList"
                                                     GroupType="TopSystem"
                                                     DataContext="{Binding}"/>

                    <!-- 2. REGULAR USER GROUPS -->
                    <navigationViews:SystemGroupsList x:Name="RegularGroupsList"
                                                     GroupType="Regular"
                                                     DataContext="{Binding}"/>

                </StackPanel>
            </ScrollViewer>

            <!-- 3. BOTTOM SYSTEM GROUPS: Archive, Trash -->
            <Border Grid.Row="3" 
                    Background="{DynamicResource SidebarColor}" 
                    Margin="24,8,24,20"
                    BorderBrush="{DynamicResource BorderColor}"
                    BorderThickness="0,1,0,0"
                    Padding="0,8,0,0">
                <navigationViews:SystemGroupsList x:Name="BottomSystemGroupsList"
                                                 GroupType="BottomSystem"
                                                 DataContext="{Binding}"/>
            </Border>

            <!-- Add Group Button -->
            <!-- <navigationViews:AddGroupButton Grid.Row="4"/> -->
        </Grid>
    </Border>

</UserControl>