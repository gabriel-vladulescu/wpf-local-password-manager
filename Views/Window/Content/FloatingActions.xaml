<UserControl x:Class="AccountManager.Views.Window.Content.FloatingActions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <!-- FLOATING ACTION BUTTON -->
    <Button Style="{StaticResource FABStyle}"
            Command="{Binding AddAccountCommand}"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Margin="0,0,32,32"
            ToolTip="Add New Account"
            Visibility="{Binding ShowAddAccountButton, Converter={StaticResource BooleanToVisibilityConverter}}">
        <materialDesign:PackIcon Kind="Plus" 
                                Foreground="White"
                                Width="24" Height="24"/>
    </Button>

</UserControl>