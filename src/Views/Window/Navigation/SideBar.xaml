<UserControl x:Class="AccountManager.Views.Window.Navigation.Sidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:navigationViews="clr-namespace:AccountManager.Views.Window.Navigation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             DataContext="{Binding SidebarViewModel}">

    <Border Background="{DynamicResource SidebarColor}" 
            CornerRadius="0">
        <Border.Effect>
            <DropShadowEffect Color="Black" 
                             Opacity="0.08" 
                             ShadowDepth="4" 
                             BlurRadius="12"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Enhanced Logo and Title -->
            <navigationViews:SidebarTop Grid.Row="0"/>
            
            <!-- Modern Groups Header -->
            <navigationViews:SidebarTopActions Grid.Row="1"/>

            <!-- Groups List with Enhanced Scrolling -->
            <ScrollViewer Grid.Row="2" 
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         Margin="20,1,20,0"
                         Style="{StaticResource ModernScrollViewer}">
                <StackPanel>

                    <!-- 1. TOP SYSTEM GROUPS with modern spacing -->
                    <navigationViews:SystemGroupsList x:Name="TopSystemGroupsList"
                                                     GroupType="TopSystem"
                                                     DataContext="{Binding}"
                                                     Margin="0,0,0,0"/>

                    <!-- 2. REGULAR USER GROUPS with enhanced styling -->
                    <navigationViews:SystemGroupsList x:Name="RegularGroupsList"
                                                     GroupType="Regular"
                                                     DataContext="{Binding}"
                                                     Margin="0,0,0,0"/>

                </StackPanel>
            </ScrollViewer>

            <!-- 3. BOTTOM SYSTEM GROUPS with modern separator -->
            <Border Grid.Row="3" 
                    Margin="20,4,20,16"
                    BorderBrush="{DynamicResource BorderColor}"
                    BorderThickness="0,1,0,0"
                    Padding="0,16,0,0"
                    Background="{DynamicResource SidebarColor}">
                
                <navigationViews:SystemGroupsList x:Name="BottomSystemGroupsList"
                                                 GroupType="BottomSystem"
                                                 DataContext="{Binding}"/>
            </Border>

        </Grid>
    </Border>

</UserControl>